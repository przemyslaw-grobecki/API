export default abstract class BasePokeResource {
    protected HttpGet() {

    }
    protected HttpPost(route: string) : void {
    
    }

    protected HttpDelete(route: string) : void {

    }

    protected HttpPatch(route: string) : void {

    }
}